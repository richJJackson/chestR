% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chestr-test.R
\name{chestr_statistics}
\alias{chestr_statistics}
\title{Global test statistics from a chestr fit}
\usage{
chestr_statistics(x, treat_term = NULL, reliable_only = TRUE)
}
\arguments{
\item{x}{A \code{"chestr"} object from \code{\link[=chestr]{chestr()}}.}

\item{treat_term}{Column name in \code{x$estimates} for the treatment effect
(e.g. \code{"treat_fGEM"} or \code{"trt"}). If \code{NULL}, uses \code{x$treat_term} or an
inferred single treatment coefficient.}

\item{reliable_only}{If \code{TRUE} (default), only reliable grid points enter
the statistics.}
}
\value{
A list with \code{T_L2}, \code{T_MAX}, \code{beta_global}, \code{n_points}, and
\code{treat_term}.
}
\description{
Computes the weighted L2 and maximum standardised deviation of local
treatment coefficients from the global treatment coefficient.
}
\details{
Let \eqn{\hat\beta_A} be the global treatment coefficient and
\eqn{\hat\beta_A(g)} the local coefficient at grid point \eqn{g}. Define
\deqn{T_{L2} = \sum_g \omega(g)\,\{\hat\beta_A(g)-\hat\beta_A\}^2}{T_L2 = sum_g w(g) * (beta(g) - beta)^2}
with weights \eqn{\omega(g)} proportional to Kish ESS events at \eqn{g}, and
\deqn{T_{MAX} = \max_g |z(g)|}{T_MAX = max |z(g)|}
where \eqn{z(g) = \{\hat\beta_A(g)-\hat\beta_A\} / \mathrm{SE}\{\hat\beta_A(g)\}}.
}
\seealso{
\code{\link[=chestr_test]{chestr_test()}}, \code{\link[=chestr]{chestr()}}
}

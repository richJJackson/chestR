% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chestr-test.R
\name{chestr_test}
\alias{chestr_test}
\title{Permutation test for treatment-effect heterogeneity}
\usage{
chestr_test(
  x,
  treat_term = NULL,
  data = NULL,
  treat_var = NULL,
  B = 199,
  seed = NULL,
  reliable_only = TRUE
)
}
\arguments{
\item{x}{A \code{"chestr"} object from \code{\link[=chestr]{chestr()}}.}

\item{treat_term}{Treatment coefficient name. Optional if stored on \code{x} or
uniquely inferable.}

\item{data}{Analysis data frame. Optional if \code{x$data} is present or the
data can be recovered from \code{x$base$call}.}

\item{treat_var}{Treatment column in \code{data}. Optional if inferable.}

\item{B}{Number of permutations (default 199).}

\item{seed}{Optional random seed for reproducibility.}

\item{reliable_only}{Passed through to \code{\link[=chestr_statistics]{chestr_statistics()}} and used when
rebuilding each permuted \code{\link[=chestr]{chestr()}} fit (same ESS rule as \code{x}).}
}
\value{
An object of class \code{"chestr_test"} with observed statistics,
permutation p-values, and null draws.
}
\description{
Tests the null that the treatment effect is constant over biomarker space
by permuting treatment labels, re-fitting the global Cox model and
\code{\link[=chestr]{chestr()}} surface, and comparing observed \code{\link[=chestr_statistics]{chestr_statistics()}} values to
the permutation null.
}
\details{
Where possible, arguments are taken from \code{x}:
\itemize{
\item \code{treat_term} from \code{x$treat_term} (or inferred)
\item \code{data} from \code{x$data} (stored by \code{\link[=chestr]{chestr()}} when recoverable)
\item \code{treat_var} inferred from \code{treat_term}
\item grid / kernel / ESS settings from \code{x}
}
}
\examples{
\donttest{
library(survival)
set.seed(1)
n <- 80
dat <- data.frame(
  time = rexp(n, 0.15), status = 1L,
  trt = rbinom(n, 1, 0.5),
  b1 = rnorm(n), b2 = rnorm(n)
)
dat$status <- as.integer(dat$time < 4)
dat$time[dat$status == 0L] <- 4
base <- coxph(Surv(time, status) ~ trt, data = dat)
cr <- chestr(base, dat[, c("b1", "b2")], grid.size = 5,
             min_events_per_df = 0, treat_term = "trt")
# Uses cr$data, cr$treat_term, and fit settings from cr:
tst <- chestr_test(cr, B = 19, seed = 1)
tst
}

}
\seealso{
\code{\link[=chestr_statistics]{chestr_statistics()}}, \code{\link[=chestr]{chestr()}}
}

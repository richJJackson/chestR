% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chestr.R
\name{chestr}
\alias{chestr}
\title{Kernel-weighted Cox estimates over a biomarker grid}
\usage{
chestr(
  base,
  biom,
  grid.size = 25,
  method = c("distance", "square_distance", "legacy"),
  kern.adj = 4,
  bw = NULL,
  min_events_per_df = 10,
  treat_term = NULL
)
}
\arguments{
\item{base}{A fitted \code{coxph} object (the global model).}

\item{biom}{Biomarker values: a vector, matrix, or data.frame (1--2 columns
supported for \code{\link[=plot_chestr]{plot_chestr()}}).}

\item{grid.size}{Number of grid points per biomarker dimension.}

\item{method}{Weighting scheme:
\itemize{
\item \code{"distance"} (default): scaled biomarkers + Gaussian kernel.
\item \code{"square_distance"}: inverse-square distance weights.
\item \code{"legacy"}: unscaled Gaussian kernel; use \code{kern.adj} for bandwidth.
}}

\item{kern.adj}{Bandwidth divisor when \code{method = "legacy"}. Larger values
give narrower kernels.}

\item{bw}{Optional kernel standard deviation; reserved for future use.}

\item{min_events_per_df}{Minimum Kish effective events per model degree of
freedom required to fit a local model (default \code{10}). Effective events use
Kish ESS among event weights: \verb{(sum w_e)^2 / sum(w_e^2)}. Grid points below
the threshold are skipped (coefficients set to \code{NA}) and a warning is
issued. Set to \code{0} or \code{NULL} to disable.}

\item{treat_term}{Optional name of the treatment coefficient to record on the
returned object (used as the default in \code{\link[=plot.chestr]{plot.chestr()}} / \code{\link[=chestr_test]{chestr_test()}}).}
}
\value{
An object of class \code{"chestr"}: a list with
\itemize{
\item \code{estimates}: data.frame of grid coordinates, coefficients, SEs, ESS columns
\item \code{base}: the fitted \code{coxph} model
\item \code{biom}: biomarker data.frame used for weighting / plotting
\item \code{data}: analysis data recovered from \code{base} when available (for permutation tests)
\item \code{treat_term}: optional treatment coefficient name
\item \code{method}, \code{kern.adj}, \code{grid.size}, \code{min_events_per_df}: fitting settings
}
}
\description{
Fits a weighted Cox model at each point on a biomarker grid. Observations
near the grid point receive higher weight, yielding local estimates of
treatment and covariate effects.
}
\examples{
\donttest{
library(survival)
set.seed(1)
n <- 60
dat <- data.frame(
  time = rexp(n, 0.1), status = 1L,
  trt = rbinom(n, 1, 0.5), biom = rnorm(n)
)
dat$status <- as.integer(dat$time < 5)
dat$time[dat$status == 0L] <- 5
base <- coxph(Surv(time, status) ~ trt, data = dat)
cr <- chestr(base, dat$biom, grid.size = 5, min_events_per_df = 0,
             treat_term = "trt")
head(cr$estimates)
plot(cr, trt.param = "trt")
}

}
\seealso{
\code{\link[=chestr_point]{chestr_point()}}, \code{\link[=plot.chestr]{plot.chestr()}}, \code{\link[=chestr_test]{chestr_test()}}, \code{\link[=grid_biom]{grid_biom()}}
}
